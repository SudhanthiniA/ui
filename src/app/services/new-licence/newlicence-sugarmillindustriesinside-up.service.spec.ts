import { TestBed } from '@angular/core/testing';

import { NewlicenceSugarmillindustriesinsideUPService } from './newlicence-sugarmillindustriesinside-up.service';

describe('NewlicenceSugarmillindustriesinsideUPService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: NewlicenceSugarmillindustriesinsideUPService = TestBed.get(NewlicenceSugarmillindustriesinsideUPService);
    expect(service).toBeTruthy();
  });
});
